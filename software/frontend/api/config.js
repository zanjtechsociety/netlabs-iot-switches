const CONFIG = {
    // Set to false to use real hardware via Fetch API
    useSimulation: true,
    
    // IP address of the ESP32/ESP8266
    deviceIp: "http://192.168.1.100/api"
};

export default CONFIG;